import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.painelpremium.app',
  appName: 'Painel Premium',
  webDir: 'dist/public'
};

export default config;
